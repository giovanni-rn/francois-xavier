// TYPES SIMPLES
const nombre = 23 // Number
const chaineDeCaracteres = "Salut !" // String
const booleen = false // boolean
let nonDefinie // undefined (aucune valeur stockée)
const rien = null // null (seule valeur possible)

// TYPES COMPLEXES
const liste = [45, "Pomme", 12, true] // array (autrement appelé "tableau")
const objet = { prenom: "François-Xavier", nom: "Pozin", age: 39 } // object (autrement appelé "dictionnaire")
function fonctionClassique(x, y) { return x + y } // function (comme il n'y a qu'1 instruction, on peut l'écrire sur 1 ligne)
const fonctionFlechee = (x, y) => x + y // arrow function (on la verra plus tard)